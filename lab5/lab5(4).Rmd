---
title: "Лабораторная работа 3"
output: html_notebook
---

```{r}
#1
  n <- 1000
  k <- seq(0, n, by = 2)
  p <- 0.3
  x <- rbinom(k, size=n, prob=p)

  
  mu <- mean(x)
  
  
  P_n_nu = sum((nu-n*p)^2 / (n*p))
```

